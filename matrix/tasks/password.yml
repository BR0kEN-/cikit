---
- name: "{{ title | default('Generate password') }}"
  set_fact: {
    "{{ name }}": "{{ for if for else lookup('password', '../cikit-credentials/' + inventory_hostname + '/' + name + ' length=16') }}"
  }
