---
jenkins_dest: /opt/jenkins
jenkins_lib: /var/lib/jenkins
jenkins:
  cli: "{{ jenkins_dest }}/jenkins-cli.jar"
  updates:
    file: "{{ jenkins_dest }}/updates_jenkins.json"
    url: "https://updates.jenkins-ci.org"
